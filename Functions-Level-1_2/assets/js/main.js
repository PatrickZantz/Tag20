function intro2(paramName) {
  const name = "SuperCoder:in";
  const message = `Hi ${name}. Mein Name ist ${paramName}.`;
  console.log(message);
  // document.getElementById("output").textContent = message;
}
